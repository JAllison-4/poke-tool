import { useEffect, useState } from "react";

function TeamSelector(){
    // Placeholder component to avoid runtime errors
    return (
        <div>
            Team selector coming soon.
        </div>
    );
}

export default TeamSelector;

